import React, { useState } from "react";
import { StyledSideContainer } from "./SideElements";

export default function Side({ children }) {
  const [isMounted, setIsMounted] = useState(!isHome);
  return <div>index</div>;
}
